## Install a Gene

1. Browse the Gene Registry in the sidebar
2. Right-click any Gene → **Install Gene**
3. The Gene downloads to your `genes/` directory
4. Use it in your Agent configuration

Genes are self-contained — each one includes its logic, schema, and metadata.
