[package]
name = "rotifer-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Core library for the Rotifer Protocol — types, sandbox, arena, algebra, fitness, IR compiler"
repository.workspace = true
homepage = "https://rotifer.dev"
readme = "README.md"
keywords = ["ai", "agent", "wasm", "evolution", "gene"]
categories = ["wasm", "science"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
anyhow.workspace = true
rusqlite.workspace = true
wasmtime.workspace = true
chrono.workspace = true
uuid.workspace = true
tracing.workspace = true
hex.workspace = true
rmp-serde.workspace = true
wasm-encoder.workspace = true
wasmparser.workspace = true
tokio.workspace = true
base64.workspace = true
# NOTE: `dns` + `mdns` features intentionally omitted — both pull
# hickory-proto, which has open advisories (RUSTSEC-2026-0118 has no fix).
# Neither is used by the implemented kad/gossipsub paths; re-add with a
# vetted resolver when DNS bootstrap / mDNS discovery is actually wired.
libp2p = { workspace = true, features = [
    "tcp",
    "quic",
    "noise",
    "yamux",
    "gossipsub",
    "kad",
    "identify",
    "memory-connection-limits",
    "tokio",
    "ed25519",
    "macros",
] }

[features]
# Opt-in marker for the multi-node P2P integration tests in src/p2p/. Those tests
# are `#[cfg_attr(not(feature = "p2p-integration"), ignore)]` — ignored by default
# (so `cargo test` stays fast + deterministic) and run in the dedicated CI job via
# `cargo test --features p2p-integration -- --test-threads=1`. Empty: no build effect.
p2p-integration = []

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
tempfile = "3"
