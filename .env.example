# Rotifer Cloud connection (optional, uses defaults if unset)
ROTIFER_CLOUD_ENDPOINT=https://cloud.rotifer.dev
ROTIFER_CLOUD_ANON_KEY=your-anon-key

# Badge token (for `vg badge` command, optional)
ROTIFER_BADGE_TOKEN=
