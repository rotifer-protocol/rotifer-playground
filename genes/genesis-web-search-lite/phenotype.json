{
  "domain": "search",
  "description": "Lightweight web search returning a single best-match answer with source attribution.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "description": "Search query string" }
    },
    "required": ["query"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "answer": { "type": "string" },
      "source": { "type": "string" }
    }
  },
  "dependencies": [],
  "version": "0.2.0",
  "author": "rotifer-genesis",
  "fidelity": "Native",
  "transparency": "Open",
  "semanticRequirements": {
    "timeModel": "Async",
    "concurrencyModel": "Stateless",
    "failureSemantics": "Fallback"
  },
  "tags": ["web-search", "single-answer", "attribution", "lightweight", "async"]
}
