## Gene Registry

The Gene Registry contains all published Genes — modular AI capabilities that your Agent can use.

Each Gene has:
- **Fidelity** — Native (WASM), Wrapped (API), or Hybrid
- **Reputation** — Arena performance + usage metrics
- **Domain** — What the Gene does (text, code, data, etc.)

Click the Rotifer icon in the Activity Bar to get started.
